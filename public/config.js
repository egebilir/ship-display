// Server configuration
const CONFIG = {
    // Server settings
    server: {
        // Use relative URL for API calls - this will work with any domain
        apiUrl: '/api'
    },
    
    // Map settings
    map: {
        defaultZoom: 2,
        updateInterval: 3000 // milliseconds
    }
}; 